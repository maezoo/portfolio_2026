(function(){const nav=document.getElementById('nav');const menuBtn=document.getElementById('menuBtn');const reduceMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const hasGSAP=typeof window.gsap!=='undefined';const hasST=typeof window.ScrollTrigger!=='undefined';if(menuBtn&&nav){menuBtn.addEventListener('click',function(){const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(function(a){a.addEventListener('click',function(){nav.classList.remove('open');menuBtn.setAttribute('aria-expanded','false');});});}const indexTrigger=document.querySelector('[data-accordion]');if(indexTrigger){const wrap=indexTrigger.closest('.work-index');indexTrigger.addEventListener('click',function(){const open=wrap.classList.toggle('is-open');const icon=indexTrigger.querySelector('i');if(icon)icon.textContent=open?'−':'+';});}document.querySelectorAll('[data-preview]').forEach(function(item){const activate=function(){const id=item.dataset.preview;document.querySelectorAll('[data-preview]').forEach(function(el){el.classList.toggle('is-active',el===item);});document.querySelectorAll('[data-preview-panel]').forEach(function(panel){panel.classList.toggle('is-active',panel.dataset.previewPanel===id);});};item.addEventListener('mouseenter',activate);item.addEventListener('focus',activate);item.addEventListener('click',activate);});document.querySelectorAll('[data-placeholder]').forEach(function(link){link.addEventListener('click',function(e){e.preventDefault();});});const compare=document.querySelector('[data-compare]');if(compare){const overlay=compare.querySelector('.compare-overlay');const handle=compare.querySelector('button');let dragging=false;const update=function(clientX){const r=compare.getBoundingClientRect();const pct=Math.max(0,Math.min(100,((clientX-r.left)/r.width)*100));overlay.style.width=pct+'%';handle.style.left=pct+'%';};handle.addEventListener('pointerdown',function(e){dragging=true;handle.setPointerCapture(e.pointerId);});handle.addEventListener('pointermove',function(e){if(dragging)update(e.clientX);});handle.addEventListener('pointerup',function(){dragging=false;});compare.addEventListener('click',function(e){update(e.clientX);});}if(hasGSAP&&hasST&&!reduceMotion){gsap.registerPlugin(ScrollTrigger);gsap.from('.site-header',{y:-12,opacity:0,duration:.45,ease:'power2.out'});gsap.from('.home-intro-copy>*',{y:20,opacity:0,stagger:.06,duration:.55,ease:'power2.out'});const hero=document.querySelector('[data-hero-visual]');if(hero){gsap.from(hero,{y:30,opacity:0,duration:.7,ease:'power2.out'});gsap.to(hero,{yPercent:-3,ease:'none',scrollTrigger:{trigger:hero,start:'top bottom',end:'bottom top',scrub:.8}});}gsap.utils.toArray('.home-project,.work-entry,.about-block,.about-skills,.about-details article,.case-text-row,.case-decision').forEach(function(item){gsap.from(item,{y:24,opacity:0,duration:.55,ease:'power2.out',scrollTrigger:{trigger:item,start:'top 84%',once:true}});});const caseSticky=document.querySelector('[data-case-sticky]');if(caseSticky){const layers=gsap.utils.toArray('[data-case-layer]');const steps=gsap.utils.toArray('[data-case-step]');steps.forEach(function(step,i){ScrollTrigger.create({trigger:step,start:'top center',end:'bottom center',onEnter:function(){layers.forEach(function(layer,j){layer.classList.toggle('is-active',i===j);});},onEnterBack:function(){layers.forEach(function(layer,j){layer.classList.toggle('is-active',i===j);});}});});}gsap.from('.archive-row',{y:14,opacity:0,stagger:.035,duration:.4,ease:'power2.out',scrollTrigger:{trigger:'.archive-layout',start:'top 84%',once:true}});}})();