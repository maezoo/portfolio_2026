(function(){const reduceMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const hasGSAP=typeof window.gsap!=='undefined';const hasST=typeof window.ScrollTrigger!=='undefined';const nav=document.getElementById('nav');const menuBtn=document.getElementById('menuBtn');if(menuBtn&&nav){menuBtn.addEventListener('click',function(){const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(function(a){a.addEventListener('click',function(){nav.classList.remove('open');menuBtn.setAttribute('aria-expanded','false');});});}document.querySelectorAll('[data-accordion]').forEach(function(item){const trigger=item.querySelector('.accordion-trigger');if(!trigger)return;trigger.addEventListener('click',function(){const open=item.classList.toggle('is-open');const icon=trigger.querySelector('i');if(icon)icon.textContent=open?'−':'+';});});document.querySelectorAll('[data-preview]').forEach(function(item){const activate=function(){const id=item.dataset.preview;document.querySelectorAll('[data-preview]').forEach(function(el){el.classList.toggle('is-active',el===item);});document.querySelectorAll('[data-preview-panel]').forEach(function(panel){panel.classList.toggle('is-active',panel.dataset.previewPanel===id);});};item.addEventListener('mouseenter',activate);item.addEventListener('focus',activate);item.addEventListener('click',activate);});const compare=document.querySelector('[data-compare]');if(compare){const overlay=compare.querySelector('.compare-overlay');const handle=compare.querySelector('button');let dragging=false;const update=function(clientX){const r=compare.getBoundingClientRect();const pct=Math.max(0,Math.min(100,((clientX-r.left)/r.width)*100));overlay.style.width=pct+'%';handle.style.left=pct+'%';};handle.addEventListener('pointerdown',function(e){dragging=true;handle.setPointerCapture(e.pointerId);});handle.addEventListener('pointermove',function(e){if(dragging)update(e.clientX);});handle.addEventListener('pointerup',function(){dragging=false;});compare.addEventListener('click',function(e){update(e.clientX);});}if(hasGSAP&&hasST&&!reduceMotion){gsap.registerPlugin(ScrollTrigger);gsap.from('.site-header',{y:-14,opacity:0,duration:.4,ease:'power2.out'});const hero=document.querySelector('[data-hero-visual]');if(hero){gsap.from('.hero-copy>*',{y:20,opacity:0,stagger:.05,duration:.5,ease:'power2.out'});gsap.from('.hero-screen-main',{y:30,opacity:0,duration:.65,ease:'power2.out'});gsap.from('.hero-screen-sub',{x:28,opacity:0,duration:.6,delay:.08,ease:'power2.out'});gsap.from('.hero-screen-detail',{y:-20,opacity:0,duration:.6,delay:.1,ease:'power2.out'});gsap.to('.hero-screen-main',{yPercent:-4,ease:'none',scrollTrigger:{trigger:hero,start:'top bottom',end:'bottom top',scrub:.8}});gsap.to('.hero-screen-sub',{yPercent:-9,ease:'none',scrollTrigger:{trigger:hero,start:'top bottom',end:'bottom top',scrub:.9}});}gsap.utils.toArray('.feature,.work-item').forEach(function(item){const visual=item.querySelector('.feature-visual,.work-visual');const copy=item.querySelector('.feature-meta,.work-info');if(visual)gsap.from(visual,{y:28,opacity:0,duration:.6,ease:'power2.out',scrollTrigger:{trigger:item,start:'top 82%',once:true}});if(copy)gsap.from(copy,{y:18,opacity:0,duration:.5,ease:'power2.out',scrollTrigger:{trigger:item,start:'top 80%',once:true}});});gsap.utils.toArray('.about-timeline article,.case-section,.case-lead-copy').forEach(function(item){gsap.from(item,{y:22,opacity:0,duration:.5,ease:'power2.out',scrollTrigger:{trigger:item,start:'top 84%',once:true}});});const caseSticky=document.querySelector('[data-case-sticky]');if(caseSticky){const layers=gsap.utils.toArray('[data-case-layer]');const steps=gsap.utils.toArray('[data-case-step]');steps.forEach(function(step,i){ScrollTrigger.create({trigger:step,start:'top center',end:'bottom center',onEnter:function(){layers.forEach(function(layer,j){layer.classList.toggle('is-active',i===j);});},onEnterBack:function(){layers.forEach(function(layer,j){layer.classList.toggle('is-active',i===j);});}});});}gsap.from('.archive-item',{y:16,opacity:0,stagger:.03,duration:.4,ease:'power2.out',scrollTrigger:{trigger:'.archive-stage',start:'top 82%',once:true}});}})();